import React from 'react';
import { Eye, RotateCcw } from 'lucide-react';
import { AdminDraft, AdminScene } from '../../sync/cloudClient';
import { resolveStoryTokens, TestPronouns } from '../../story/tokens';

const firstScene = (scenes: AdminScene[]) => [...scenes].sort((a, b) => a.chapterNumber - b.chapterNumber)[0] ?? null;

export const DraftPlaytest: React.FC<{ draft: AdminDraft }> = ({ draft }) => {
  const scenes = draft.scenes ?? [];
  const scenesById = React.useMemo(() => new Map(scenes.map((scene) => [scene.sceneId, scene])), [scenes]);
  const [sceneId, setSceneId] = React.useState<string | null>(() => firstScene(scenes)?.sceneId ?? null);
  const [history, setHistory] = React.useState<string[]>([]);
  const [playerName, setPlayerName] = React.useState('Alex');
  const [pronouns, setPronouns] = React.useState<TestPronouns>('they');
  const [species, setSpecies] = React.useState('Human');
  React.useEffect(() => { setSceneId(firstScene(scenes)?.sceneId ?? null); setHistory([]); }, [draft.draftId, draft.revision]);
  const scene = sceneId ? scenesById.get(sceneId) ?? null : null;
  const resolve = (text: string, speakerName?: string) => resolveStoryTokens(text, playerName, pronouns, species, speakerName, draft.storyValues ?? {}, draft.relationshipValues ?? {});
  const restart = () => { setSceneId(firstScene(scenes)?.sceneId ?? null); setHistory([]); };
  const choose = (nextSceneId?: string | null) => { if (scene) { setHistory((items) => [...items, scene.sceneId]); setSceneId(nextSceneId || null); } };
  return <section className="mt-6 rounded-xl border border-[#c5a059]/30 bg-[#150f1f] p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="flex items-center gap-2 font-display text-xl text-[#f5f0e6]"><Eye className="h-5 w-5 text-[#e5c158]" />Draft playtest</h2><p className="mt-1 text-sm text-stone-400">Private preview only — no progress, player data or live story content is changed.</p></div><button onClick={restart} className="inline-flex items-center gap-1 rounded border border-white/15 px-3 py-2 text-sm text-stone-200"><RotateCcw className="h-4 w-4" />Restart</button></div><div className="mt-4 grid gap-3 rounded border border-white/10 bg-black/15 p-3 sm:grid-cols-3"><label className="text-xs text-stone-400">Test player name<input value={playerName} onChange={(event) => setPlayerName(event.target.value)} className="mt-1 w-full rounded border border-white/15 bg-black/20 p-2 text-sm text-[#f5f0e6]" /></label><label className="text-xs text-stone-400">Test pronouns<select value={pronouns} onChange={(event) => setPronouns(event.target.value as TestPronouns)} className="mt-1 w-full rounded border border-white/15 bg-black/20 p-2 text-sm text-[#f5f0e6]"><option value="they">they / them</option><option value="she">she / her</option><option value="he">he / him</option></select></label><label className="text-xs text-stone-400">Test species<input value={species} onChange={(event) => setSpecies(event.target.value)} className="mt-1 w-full rounded border border-white/15 bg-black/20 p-2 text-sm text-[#f5f0e6]" /></label><p className="sm:col-span-3 text-xs text-stone-500">Supported tokens include <code>{'{{story.key}}'}</code>, <code>{'{{relationship.key}}'}</code>, and the player and dialogue tokens.</p></div>{scenes.length === 0 ? <p className="mt-5 rounded border border-dashed border-white/15 p-4 text-sm text-stone-400">Add and save at least one scene to begin a playtest.</p> : !scene ? <div className="mt-5 rounded border border-white/15 bg-black/15 p-5"><h3 className="font-display text-xl text-[#f5f0e6]">End of this route</h3><p className="mt-2 text-sm text-stone-400">This choice has no destination scene yet.</p></div> : <div className="mt-5 rounded border border-white/10 bg-black/15 p-5"><p className="text-xs font-semibold uppercase tracking-wider text-[#c5a059]">Chapter {scene.chapterNumber}</p><h3 className="mt-2 font-display text-2xl text-[#f5f0e6]">{resolve(scene.title)}</h3><div className="mt-5 whitespace-pre-wrap leading-7 text-stone-200">{resolve(scene.body)}</div>{(scene.dialogue ?? []).length > 0 && <div className="mt-5 grid gap-3">{(scene.dialogue ?? []).map((line, index) => { const displayName = resolve(line.displayName); const initial = displayName.trim().slice(0, 1).toUpperCase() || '?'; return <article key={`${line.speakerId}-${index}`} className="rounded-lg border border-[#c5a059]/35 bg-[#25182d] p-4"><div className="flex gap-3"><div aria-label={`${displayName} placeholder portrait`} className="flex h-11 w-11 shrink-0 items-center justify-center rounded-full border border-[#e5c158]/60 bg-gradient-to-br from-[#7d0828] to-[#231632] font-display text-lg text-[#f5f0e6]">{initial}</div><div className="min-w-0 flex-1"><div className="flex items-baseline justify-between gap-3"><strong className="text-[#e5c158]">{displayName}</strong><span className="text-xs uppercase tracking-wide text-stone-400">{line.mood}</span></div><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-stone-100">{resolve(line.text, displayName)}</p></div></div></article>; })}</div>}<div className="mt-6 grid gap-3">{scene.choices.length === 0 ? <p className="text-sm text-stone-400">This scene has no choices yet.</p> : scene.choices.map((choice) => { const missingTarget = Boolean(choice.nextSceneId && !scenesById.has(choice.nextSceneId)); return <button key={choice.choiceId} disabled={missingTarget} onClick={() => choose(choice.nextSceneId)} className="rounded-lg border border-[#c5a059]/40 bg-[#25182d] px-4 py-3 text-left text-sm text-[#f5f0e6] transition hover:border-[#e5c158] disabled:cursor-not-allowed disabled:opacity-50"><span className="block font-semibold">{resolve(choice.text)}</span>{missingTarget && <span className="mt-1 block text-xs text-rose-300">Destination scene “{choice.nextSceneId}” has not been written yet.</span>}</button>; })}</div></div>}{history.length > 0 && <p className="mt-4 text-xs text-stone-500">Route: {history.join(' → ')}{scene ? ` → ${scene.sceneId}` : ''}</p>}</section>;
};
